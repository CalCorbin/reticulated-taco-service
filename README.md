# reticulated-taco-service

## Local setup

1. Navigate to the root of the project.
2. Start the virtual env, `source env/bin/activate`.

## Generate Requirements File

1. Run `pip freeze > requirements.txt` in project root.
